# Nome da aplica��o
spring.application.name=frota

# Configura��es de conex�o com o banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/frota?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=alunofatec

# Dialeto do Hibernate para MySQL 8
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Mostrar as queries SQL no console (pode ser �til durante o desenvolvimento)
spring.jpa.show-sql=false

# Habilitar filtro para m�todos HTTP escondidos (necess�rio para o m�todo DELETE nos formul�rios)
spring.mvc.hiddenmethod.filter.enabled=true

# Estrat�gia de DDL do Hibernate (faz com que o banco seja atualizado automaticamente com as entidades)
spring.jpa.hibernate.ddl-auto=update

# Desativar cache do Thymeleaf (importante durante o desenvolvimento para evitar cache das p�ginas)
spring.thymeleaf.cache=false

# Configura��es para arquivos est�ticos (caso tenha arquivos como CSS, JS, imagens, etc.)
spring.resources.static-locations=classpath:/static/

# Configura��o da porta do servidor
server.port=8083
